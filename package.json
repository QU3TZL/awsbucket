{
  "name": "filepond-server",
  "version": "1.0.0",
  "description": "Server-side component for FilePond file uploads",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.17.1",
    "express-fileupload": "^1.2.1",
    "scp2": "^0.5.0"
  }
}
