{
  "name": "file-upload-server",
  "version": "1.0.0",
  "description": "A server-side solution for handling file uploads from FilePond and transferring them to a Render disk.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.17.1",
    "express-fileupload": "^1.2.1",
    "scp2": "^0.5.0"
  }
}
